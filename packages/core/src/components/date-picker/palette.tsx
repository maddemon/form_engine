import type { ComponentPalette } from '../../types/palette'
import { Calendar } from '../icons'
export const datePalette: ComponentPalette = {
  label: '日期',
  category: 'form',
  icon: <Calendar />,
}
export const dateRangePalette: ComponentPalette = {
  label: '日期范围',
  category: 'form',
  icon: <Calendar />,
}